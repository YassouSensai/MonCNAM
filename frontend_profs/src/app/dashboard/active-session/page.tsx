import ActiveSessionPage from '@/features/session/components/active-session-page';

export const metadata = {
  title: 'Dashboard : Active Session'
};

export default function Page() {
  return <ActiveSessionPage />;
}
